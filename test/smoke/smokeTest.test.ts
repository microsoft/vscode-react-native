import * as assert from "assert";
import {Application} from "spectron";

suite("smokeTest", function() {
    suite("smokeContext", function () {
        let app = new Application({
            path: "",
        });
    });
});