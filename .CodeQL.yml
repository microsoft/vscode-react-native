path_classifiers:
  exclude:
    - "mochawesome-report/**" # Ignore test report directory, generated by 3rd party modules
    - "dist/**" # Ignore extension webpack directory, generated by gulp script to build .vsix file
    - ".vscode-test/**" # Ignore vscode-test directory, created by @vscode/test-electron to execute unit test