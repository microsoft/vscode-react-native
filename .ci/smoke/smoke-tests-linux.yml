trigger:
  branches:
    include: ['master']

variables:
  group: vswdbot-password

jobs:
  - job: Linux
    pool:
      name: 'React Native smoke tests - Linux'
    steps:
    - checkout: self
      clean: true
    - template: smoke-tests-common-validation.yml
