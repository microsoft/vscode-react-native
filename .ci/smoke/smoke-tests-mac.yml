trigger:
  branches:
    include: ['master']

variables:
  group: vswdbot-password

jobs:
  - job: macOS
    pool:
      name: 'React Native smoke tests - macOS'
    steps:
    - checkout: self
      clean: true
    - template: smoke-tests-common-validation.yml
